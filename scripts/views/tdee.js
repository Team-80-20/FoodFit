var app = app || {};

(module => { 

    const tdeeView= {}

    const $view = $('#tdee-view')

    tdeeView.init = () => $view.show()

    module.tdeeView = tdeeView

    tdeeCalc = (age, height, weight, activity) => {

    }

})(app)